import type { AsciiPalette, AsciiMode } from "../../types/auth";


interface AsciiProps {
  palette?: AsciiPalette;
  mode?: AsciiMode;
}

export const AsciiNumberCanvas: React.FC<AsciiProps> = () => {
  return null;
};
